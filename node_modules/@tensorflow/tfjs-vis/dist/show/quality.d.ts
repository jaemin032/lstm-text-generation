import { Drawable } from '../types';
export declare function showPerClassAccuracy(container: Drawable, classAccuracy: Array<{
    accuracy: number;
    count: number;
}>, classLabels?: string[]): Promise<void>;
export declare function showConfusionMatrix(container: Drawable, confusionMatrix: number[][], classLabels?: string[]): Promise<void>;
