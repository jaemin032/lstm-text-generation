import { Drawable, VisOptions } from '../types';
export declare function renderBarchart(data: Array<{
    index: number;
    value: number;
}>, container: Drawable, opts?: VisOptions): Promise<void>;
